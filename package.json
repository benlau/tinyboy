{
  "name": "tinyboy",
  "version": "0.0.1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "description": "Tinyboy",
  "dependencies": {
    "aws-sdk": "~1.9.0",
    "walk": "~2.2.1"
  }
}
